# Cloudflare Tunnel token — create at:
# Zero Trust → Networks → Tunnels → Create a tunnel
TUNNEL_TOKEN=your-cloudflare-tunnel-token-here

# MQTT credentials — run ./gen-passwd.sh after setting these
MQTT_NODE_PASS=changeme-node
MQTT_ADMIN_PASS=changeme-admin
